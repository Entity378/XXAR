<!-- XXAR color palette derived from the logo (indigo + lavender + silver).
     Referenced via DynamicResource from every custom dialog. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Palette -->
    <Color x:Key="XXARBgTopColor">#10123A</Color>
    <Color x:Key="XXARBgBottomColor">#05061A</Color>
    <Color x:Key="XXARSurfaceColor">#1B1E4D</Color>
    <Color x:Key="XXARAccentColor">#9090C0</Color>
    <Color x:Key="XXARAccentHoverColor">#B0B0E0</Color>
    <Color x:Key="XXARAccentPressedColor">#7070A8</Color>
    <Color x:Key="XXARTextPrimaryColor">#F0F0F5</Color>
    <Color x:Key="XXARTextSecondaryColor">#B0B0C8</Color>
    <Color x:Key="XXARBorderColor">#2E316A</Color>

    <!-- Brushes -->
    <LinearGradientBrush x:Key="XXARWindowBg" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource XXARBgTopColor}" Offset="0" />
        <GradientStop Color="{StaticResource XXARBgBottomColor}" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="XXARSurfaceBrush" Color="{StaticResource XXARSurfaceColor}" />
    <SolidColorBrush x:Key="XXARAccentBrush" Color="{StaticResource XXARAccentColor}" />
    <SolidColorBrush x:Key="XXARAccentHoverBrush" Color="{StaticResource XXARAccentHoverColor}" />
    <SolidColorBrush x:Key="XXARAccentPressedBrush" Color="{StaticResource XXARAccentPressedColor}" />
    <SolidColorBrush x:Key="XXARTextPrimaryBrush" Color="{StaticResource XXARTextPrimaryColor}" />
    <SolidColorBrush x:Key="XXARTextSecondaryBrush" Color="{StaticResource XXARTextSecondaryColor}" />
    <SolidColorBrush x:Key="XXARBorderBrush" Color="{StaticResource XXARBorderColor}" />

    <!-- Primary button (accent) -->
    <Style x:Key="XXARPrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource XXARAccentBrush}" />
        <Setter Property="Foreground" Value="#0B0D24" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="22,8" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="96" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background"
                                    Value="{DynamicResource XXARAccentHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background"
                                    Value="{DynamicResource XXARAccentPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Secondary button (outlined) -->
    <Style x:Key="XXARSecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource XXARTextPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource XXARBorderBrush}" />
        <Setter Property="Padding" Value="22,8" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="96" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#222554" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Dark TextBox -->
    <Style x:Key="XXARTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="#0E1030" />
        <Setter Property="Foreground" Value="{DynamicResource XXARTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource XXARBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="CaretBrush" Value="{DynamicResource XXARAccentBrush}" />
    </Style>

    <!-- Heading / body text -->
    <Style x:Key="XXARHeading" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource XXARTextPrimaryBrush}" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="Light" />
    </Style>
    <Style x:Key="XXARBody" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource XXARTextSecondaryBrush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="20" />
    </Style>

</ResourceDictionary>
